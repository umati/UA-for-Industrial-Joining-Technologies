<Project>
  <PropertyGroup>
    <!-- Generate NuGet lock files for deterministic restore in CI/local -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>
</Project>
