# Core runtime dependencies (Python 3.14+ compatible)
# Install with: pip install -c <repo>/constraints.txt -r requirements.txt
# constraints.txt enforces repo-wide transitive dependency security floors.
#
# asyncua is pinned in repo-root constraints.txt to upstream SHA 35a77c6b
# (post-1.2b2; 2026-05-11) for Python 3.14.4 compatibility.
asyncua
websockets~=16.0
python-dotenv~=1.2
packaging~=26.0

# Imported by runtime helper modules
pytz~=2026.1
aiofiles~=25.1
