# Core runtime dependencies (Python 3.14+ compatible)
# asyncua >= 1.2b2 is required for Python 3.14 support; installed via setup_project.py
# with --pre so pre-releases are found; once 1.2.x stable ships, pip picks that automatically.
asyncua>=1.2b2
websockets~=16.0
python-dotenv~=1.2
packaging~=26.0

# Imported by runtime helper modules
pytz~=2026.1
aiofiles~=25.1
