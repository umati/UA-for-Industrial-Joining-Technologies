# IJT Web Client Configuration

- Edit the default runtime templates in `src/resources/` for shared defaults.
- Keep local runtime JSON files uncommitted.
- Use the browser README for quick start and this file for deeper setup details.
