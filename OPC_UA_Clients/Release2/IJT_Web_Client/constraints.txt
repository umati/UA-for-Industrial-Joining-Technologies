# Web Client local constraints mirror of <repo-root>/constraints.txt.
# Keep this file in sync with repo-root constraints so local installs run from
# this directory receive the same security floors and asyncua compatibility pin.
#
# Install from this directory with:
#   pip install -c constraints.txt -r requirements.txt
# or
#   pip install -c constraints.txt -r requirements-dev.txt

# asyncua is pinned to the latest released PyPI build across IJT Python clients.
# Current target:
#   * asyncua 2.0.1 (released baseline replacing the earlier temporary SHA pin)
#   * keeps Python 3.14 support in a tagged build instead of an unreleased commit
# Validate method calls, custom structures, subscriptions, and type loading when bumping.
asyncua==2.0.1

# cryptography and pyOpenSSL share a tightly-coupled version contract:
# each pyOpenSSL release caps the cryptography minor it accepts.
# Pinning them with ~= ceilings
# in requirements.txt causes impossible pip resolves whenever one is bumped
# without the other.  Instead we list only security floors here and let pip's
# resolver enforce mutual compatibility.  Bump these floors together when a
# new pyOpenSSL release extends its cryptography upper bound.
cryptography>=50.0.0
pyOpenSSL>=26.4.0

# CVE-2026-45409 / GHSA-65pc-fj4g-8rjx
idna>=3.15

# PYSEC-2026-161 — starlette < 1.0.1 vulnerable; pulled transitively by mcp / sse-starlette.
starlette>=1.0.1
