interface:
  display_name: "SimulateSingleResult Caller"
  short_description: "Invoke SimulateSingleResult reliably."
  default_prompt: "Use $simulate-single-result-caller to wire and invoke the SimulateSingleResult OPC UA method from the Methods view with safe defaults."
