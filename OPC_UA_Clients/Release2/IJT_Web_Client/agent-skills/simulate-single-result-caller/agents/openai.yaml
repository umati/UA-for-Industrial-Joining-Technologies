interface:
  display_name: "SimulateSingleResult Caller"
  short_description: "Wire and invoke SimulateSingleResult."
  default_prompt: "Use $simulate-single-result-caller to expose, configure, and call SimulateSingleResult from the Methods view using safe defaults and existing MethodManager flow."
