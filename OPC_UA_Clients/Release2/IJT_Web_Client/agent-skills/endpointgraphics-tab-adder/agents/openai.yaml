interface:
  display_name: "EndpointGraphics Tab Adder"
  short_description: "Add tabs in EndpointGraphics safely."
  default_prompt: "Use $endpointgraphics-tab-adder to add a new tab in EndpointGraphics.mjs with the correct import, initialization pattern, and generateTab wiring."
