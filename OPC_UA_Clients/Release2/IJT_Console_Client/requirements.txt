# Python 3.14+ compatible requirements
# asyncua >= 1.2b2 is required for Python 3.14 support; installed with --pre via setup_client.py
# so pre-releases are found; once asyncua 1.2.x stable ships pip picks that automatically.
asyncua>=1.2b2
pytz~=2026.1
aiofiles~=25.1
orjson~=3.11
cryptography~=48.0
pyOpenSSL~=26.0
