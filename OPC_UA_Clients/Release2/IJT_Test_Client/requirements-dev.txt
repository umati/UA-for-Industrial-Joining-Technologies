# Install with: pip install -c <repo>/constraints.txt -r requirements-dev.txt
# constraints.txt enforces repo-wide transitive dependency security floors.
pytest~=9.0
pytest-asyncio~=1.3
pytest-timeout~=2.4
pytest-xdist~=3.0
pytest-cov~=7.1
pytest-html~=4.0
coverage[toml]~=7.0
defusedxml~=0.7
openpyxl~=3.0
# linting & formatting
pre-commit~=4.0
ruff~=0.15
# type checking
mypy~=2.2.0
# security & quality
bandit~=1.9
pip-audit~=2.9
pyright~=1.1
detect-secrets~=1.5
# Security floor for transitive requests/pip-audit dependency.
urllib3>=2.7.0
# Optional tools (auto-detected by runner, install separately if desired):
#   semgrep    — pip install semgrep  (requires Python <3.14 or pre-built wheel)
