pytest~=9.0
pytest-asyncio~=1.3
pytest-timeout~=2.4
pytest-cov~=6.0
coverage[toml]~=7.0
# linting & formatting
ruff~=0.15
pylint~=4.0
# type checking
mypy~=1.15
# security & quality
bandit~=1.9
pip-audit~=2.9
vulture~=2.14
interrogate~=1.7
# Optional tools (auto-detected by runner, install separately if desired):
#   semgrep    — pip install semgrep  (requires Python <3.14 or pre-built wheel)
#   pyright    — npm install -g pyright  OR  pip install pyright
#   detect-secrets — pip install detect-secrets
