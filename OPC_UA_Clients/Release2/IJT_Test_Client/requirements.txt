# Install with: pip install -c <repo>/constraints.txt -r requirements.txt
# constraints.txt enforces repo-wide transitive dependency security floors.
# asyncua >= 1.2b2 is required for Python 3.14 support; installed with --pre via conftest bootstrap.
# Once asyncua 1.2.x stable ships, pip picks it automatically and this comment can be removed.
asyncua>=1.2b2
PyYAML>=6.0
